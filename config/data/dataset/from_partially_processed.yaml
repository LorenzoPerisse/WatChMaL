# Note : 
#   Config to use for a graph dataset
#   that hasn't been filter nor egdes
#   (meaning : .root -> data.pt)


# Watchmal arguments
split_path: /sps/t2k/eleblevec/updated_watchmal/index_lists/e_mu_500MeV_10each_train_val_test_index_list.npz
fully_processed: False

# if NOT fully_processed this will be called
compute_edges_parameters:
  k: 2
  num_workers: 1
  cosine: False

dataset_parameters:

  # --- Hydra arguments ---
  _target_: watchmal.dataset.gnn.dataset_from_processed.DatasetFromProcessed

  # --- GraphDataset argument ---
  graph_folder_path: /sps/t2k/eleblevec/datasets/graph_datasets/e_mu_500MeV_10each
  graph_file_names:
    - data.pt
  
  verbose: 1
  
  # --- End ---




